
import React from 'react';
import Navbar from '@/components/navigation';

// This is a compatibility component to handle existing imports
// It simply re-exports the Navbar component from the navigation folder
export default Navbar;
